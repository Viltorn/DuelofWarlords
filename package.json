{
  "name": "duelofwarlords",
  "version": "1.0.0",
  "description": "tcg  game",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build-client && cd server && node server",
    "start-local": "npm run start-server && npm run serve-build",
    "serve-build": "serve -s ./client/build",
    "start-server": "cd server && PORT=8080 node server",
    "build-client": "cd client && npm run build && cd .."
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Viltorn/DuelofWarlords.git"
  },
  "keywords": [
    "game"
  ],
  "author": "Viltorn",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Viltorn/DuelofWarlords/issues"
  },
  "homepage": "https://github.com/Viltorn/DuelofWarlords#readme",
  "engines": {
    "node": "19.4.0"
  }
}
