// import styles from './ImagesList.module.css';

// const ImagesLIst = ({ itemList, number }) => {
//   const offset = 120 * number;

//   return (
//     <div className={styles.container} style={{ transform: `translateX(${offset}px` }}>
//       {itemList.map((item) => (
//       ))}
//     </div>
//   );
// };

// export default ImagesLIst;
